doctype html
html(lang="jp")
  head
    meta(charset="UTF-8")
    title 秘密の匿名掲示板
  body
    h1 秘密の匿名掲示板
    a(href="/logout") ログアウト
    h2 新規投稿
    form(method="post" action="/posts")
      div
        textarea(name="content" cols=40 rows=4)
      select(name="which" class="select-board")
        option(value="Japanesefood") 和食
        option(value="Westernfood") 洋食
      div
        button(type="submit") 投稿
    h2 投稿一覧
    each food in foods
      if food.foodstyle === "Japanesefood"
        p 和食
        p #{food.foodname}
      if food.foodstyle === "Westernfood"
        p 洋食
        p #{food.foodname}